### The events for 'German Flagswitch'
### By MEEEB
### v2
### restricted gerflag.2-5 to a fascist govt
### Added gerflag.6 for Imperial Germany

### Create a namespace for this

add_namespace = gerflag

### done

### Uncle A and Kaiser Willy are supported
### Will probably get more complicated since you can freely puppet everything in vanilla


### Start of the event for the civil flag

country_event = {
	id = gerflag.1
	immediate = {log = "[GetDateText]: [Root.GetName]: event gerflag.1"}
	title = gerflag.1.t
	desc = gerflag.1.d

	trigger = {
	tag = GER
	has_country_flag = GER_war_flag # War flag must be set --> doesn't trigger at game-start
	NOT = {
	has_war = yes # looks wonky but works
	}
	}

	mean_time_to_happen = {
	days = 10
	}

	option = {
	name = gerflag.1.a
	clr_country_flag = GER_war_flag
	set_country_flag = GER_civil_flag
	set_cosmetic_tag = GER_at_peace
	}
}

### End of the event for the civil flag


### Start of the event for the war flag (1935-1938)

country_event = {
	id = gerflag.2
	immediate = {log = "[GetDateText]: [Root.GetName]: event gerflag.2"}
	title = gerflag.2.t
	desc = gerflag.2.d

	trigger = {
	tag = GER
	has_government = fascism
	date < 1938.01.01
	#has_country_flag = GER_civil_flag # Commented out so we don't need a third event
	NOT = {
	has_country_flag = GER_war_flag
	}
	has_war = yes
	}

	mean_time_to_happen = {
	days = 10
	}

	option = {
	name = gerflag.2.a
	clr_country_flag = GER_civil_flag # dirty but works
	set_country_flag = GER_war_flag
	set_cosmetic_tag = GER_at_war1 
	}
}

### End of the event for the war flag (1935-1938)

### Start of the event for the war flag (1938-1945, but you might last longer)

country_event = {
	id = gerflag.3
	immediate = {log = "[GetDateText]: [RootGetName]: event gerflag.3"}
	title = gerflag.3.t
	desc = gerflag.3.d

	trigger = {
	tag = GER
	has_government = fascism
	date > 1937.12.31
	#has_country_flag = GER_civil_flag # Will keep this for reasons
	NOT = {
	has_country_flag = GER_war_flag
	}
	has_war = yes
	}

	mean_time_to_happen = {
	days = 10
	}

	option = {
	name = gerflag.3.a
	clr_country_flag = GER_civil_flag # still dirty
	set_country_flag = GER_war_flag
	set_country_flag = GER_war_flag_switch_no
	set_cosmetic_tag = GER_at_war2
	}
}

### End of the event for the war flag (1938-1945)

### Start of the event for the war flag switch (players like war)

country_event = {
	id = gerflag.4
	immediate = {log = "[GetDateText]: [RootGetName]: event gerflag.4"}
	title = gerflag.4.t
	desc = gerflag.4.d

	fire_only_once = yes

	trigger = {
	tag = GER
	has_government = fascism
	date > 1937.12.31
	has_country_flag = GER_war_flag
	NOT = {
	has_country_flag = GER_war_flag_switch_no
	}
	has_war = yes
	}

	mean_time_to_happen = {
	days = 10
	}

	option = {
	name = gerflag.4.a
	set_cosmetic_tag = GER_at_war2
	}

	option = {
	name = gerflag.4.b
	}
}

### End of the event for the war flag switch

### Start of the event to prevent gerflag.3 and .4 occuring simultaneously

country_event = {
	id = gerflag.5
	immediate = {log = "[GetDateText]: [RootGetName]: event gerflag.5"}
	title = gerflag.5.t
	desc = gerflag.5.d

	fire_only_once = yes

	trigger = {
	tag = GER
	has_government = fascism
	date > 1937.12.31
	NOT = {
	has_country_flag = GER_war_flag
	has_war = yes
	}
	}

	mean_time_to_happen = {
	days = 10
	}

	option = {
	name = gerflag.5.a
	set_country_flag = GER_war_flag_switch_no
	}
}

### End of the prevention

### War flag for Mr. Hat, Willy and Willy Jr.

country_event = {
	id = gerflag.6
	immediate = {log = "[GetDateText]: [RootGetName]: event gerflag.6"}
	title = gerflag.6.t
	desc = gerflag.6.d

	trigger = {
	tag = GER
	has_government = neutrality # For August or Willy
	NOT = {
	has_country_flag = GER_war_flag
	}
	has_war = yes
	}

	mean_time_to_happen = {
	days = 10
	}

	option = {
	name = gerflag.6.a
	clr_country_flag = GER_civil_flag # still works
	set_country_flag = GER_war_flag
	set_cosmetic_tag = GER_at_war1
	}
}

### End of Willy's War flag

### Todo:
### Rework loc
### Block these events for Dems, Commies (maybe)
### Give the fascist rebels (Oppose Hitler) the ability to use this
### Großdeutsches Reich, probably
### Make the flag changes (war-peace, peace-war) optional
### Tooltips
### Give the communists a choice? The had quite a lot of flags (historical GDR)